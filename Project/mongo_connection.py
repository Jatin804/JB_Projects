# import pymongo

# url = "mongodb://localhost:27017"       # url of the server  
# client = pymongo.MongoClient(url)       # to call or run it

# db = client['NATURE']                   # associating db to server for connection

